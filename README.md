## Projects
### C
- `calculator_functions.c` — basic calculator using functions and switch
- `guesssingNumber_game.c` — number guessing game
- `guessingnumber_withLogin.c` — number guessing game with login system
- `addition_game.c` — simple addition practice game
- `matriz.c` — multidimensional arrays (matrices)
- `pointers.c` — pointer basics: dereferencing, pointer arithmetic with arrays, and pass-by-reference (swap, double_value)
- `count_vowels.c` — string basics: null terminator, strlen, pointer traversal, and passing strings to functions

### C++ (`/cpp`)
- `login.cpp` — login system using `std::string`/`char[]`, debugged (fixed comparison bugs and cin/fgets buffer conflict)
- `vector_practice.cpp` — practice reading user input into a `std::vector`
- `pointers.cpp` — C++ translation of pointer basics from `pointers.c`
- `count_vowels.cpp` — C++ translation of vowel counting using `std::string`
- `count_letter.cpp` — count occurrences of a specific letter in a string
- `to_uppercase.cpp` — modify a string in place using references (`&`)

## Goals
- Understand the logic behind every line of code, not just copy-paste solutions
- Practice one concept at a time, building on the previous one
- Currently learning: **Strings** (C, Chapter 12) — completed null terminator, strlen, strcat/strcpy, pointer traversal, and passing strings to functions; C++ translation done with std::string and references. Next: arrays of strings.
